spec:
  action: delay
  mode: all

  selector:
    labelSelectors:
      app: pcscf

  target:
    selector:
      labelSelectors:
        app: amf   # or smf / any other CNF

  direction: both

  delay:
    latency: "200ms"

  loss:
    loss: "5"
